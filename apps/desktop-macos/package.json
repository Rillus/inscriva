{
  "name": "@inscriva/desktop-macos",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "pnpm --filter @inscriva/web build && tauri build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@inscriva/bridge": "workspace:*",
    "@tauri-apps/api": "^2.4.0",
    "@tauri-apps/plugin-dialog": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
